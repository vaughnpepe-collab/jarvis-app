# Development / test dependencies.
# The test suite uses only the standard library, so it runs without anything here:
#     python -m unittest discover -s tests
#
# pytest is optional — it gives nicer output and runs the same tests:
#     python -m pytest tests
pytest>=7.0
