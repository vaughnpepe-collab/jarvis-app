# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/

# Runtime logs (generated by jarvis_server.py)
jarvis.log
jarvis.log.*

# Prospect data — repo is PUBLIC, so never commit leads/contacts/outreach.
# (Generated locally by the scripts; the code stays, the data stays private.)
leads/*.csv
leads/leads_*.md
leads/outreach_pack_*.md
leads/email_drafts*.json
leads/email_drafts*.md
